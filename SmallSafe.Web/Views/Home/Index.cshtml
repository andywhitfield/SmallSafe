@model SmallSafe.Web.ViewModels.Home.IndexViewModel

<article>
    <div class="ss-group-toolbar">
    <a href="TODO">Sort by name</a> | <a href="TODO">Add group</a> | <input type="text" name="filter" placeholder="Filter groups..." />
    </div>

    <ul class="ss-list">
        <li class="ss-list">
            <ul class="ss-list-item">
                <li class="ss-list-item" data-group="TODO-groupid">
                    <div><img class="ss-list-item-drag-handle" src="/images/drag.png" height="26" width="22" title="Move" /></div>
                    <div></div>
                    <div data-href="/group/1" class="ss-list-item-link">Group 1</div>
                    <div>
                        <form data-confirm="Are you sure you want to delete this group? Note that this will also delete all password entries in this group." method="post" action="TODO">
                        @Html.AntiForgeryToken()
                        <button title="Delete"><img src="/images/close.png" height="15" width="15" /> Delete</button>
                        </form>
                    </div>
                </li>
                <li class="ss-list-item" data-group="TODO-groupid">
                    <div><img class="ss-list-item-drag-handle" src="/images/drag.png" height="26" width="22" title="Move" /></div>
                    <div></div>
                    <div data-href="/group/2" class="ss-list-item-link">Group 2</div>
                    <div>
                        <form data-confirm="Are you sure you want to delete this group? Note that this will also delete all password entries in this group." method="post" action="TODO">
                        @Html.AntiForgeryToken()
                        <button title="Delete"><img src="/images/close.png" height="15" width="15" /> Delete</button>
                        </form>
                    </div>
                </li>
                <li class="ss-list-item" data-group="TODO-groupid">
                    <div><img class="ss-list-item-drag-handle" src="/images/drag.png" height="26" width="22" title="Move" /></div>
                    <div></div>
                    <div data-href="/group/3" class="ss-list-item-link">Group 3</div>
                    <div>
                        <form data-confirm="Are you sure you want to delete this group? Note that this will also delete all password entries in this group." method="post" action="TODO">
                        @Html.AntiForgeryToken()
                        <button title="Delete"><img src="/images/close.png" height="15" width="15" /> Delete</button>
                        </form>
                    </div>
                </li>
            </ul>
        </li>
    </ul>




    <hr />
    <p>
    Auth key:
    </p>
    <p>
    <img src="@Model.QrCodeImageUrl" />
    </p>
    <p>
    Manual setup code: @Model.ManualEntrySetupCode
    </p>
    <p>
    Validate code:
    <form method="POST" action="/validate">
    <input type="text" name="totp" />
    <input type="submit" value="Validate" />
    </form>
    @if (Model.ValidationResult.HasValue)
    {
        @if (Model.ValidationResult.Value) {
        <div>Pass</div>
        } else {
        <div>Fail</div>
        }
    }
    </p>
</article>